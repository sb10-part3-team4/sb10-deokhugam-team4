spring:
  test:
    database:
      replace: none
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
    show-sql: true

dashboard:
  batch:
    cron: "0 0 3 * * *"
    zone: "Asia/Seoul"
