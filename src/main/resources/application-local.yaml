spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5433/deokhugam}
    username: ${DB_USERNAME:deokhugam}
    password: ${DB_PASSWORD:deokhugam}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

logging:
  level:
    root: INFO
    com.codeit.team4: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
